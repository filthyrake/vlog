apiVersion: batch/v1
kind: CronJob
metadata:
  name: cleanup-failed-pods
  namespace: vlog
spec:
  # Run every 5 minutes
  schedule: "*/5 * * * *"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 1
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      ttlSecondsAfterFinished: 300
      template:
        spec:
          serviceAccountName: pod-cleanup
          restartPolicy: OnFailure
          # Security context for the pod
          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
            seccompProfile:
              type: RuntimeDefault
          containers:
          - name: cleanup
            # Pin kubectl version to match cluster version
            # Update this when upgrading Kubernetes
            image: bitnami/kubectl:1.31
            command:
            - /bin/sh
            - -c
            - |
              echo "Cleaning up failed/evicted pods in vlog namespace..."
              kubectl delete pods -n vlog --field-selector=status.phase=Failed --ignore-not-found
              echo "Cleanup complete"
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: pod-cleanup
  namespace: vlog
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: pod-cleanup
  namespace: vlog
rules:
- apiGroups: [""]
  resources: ["pods"]
  verbs: ["list", "delete"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: pod-cleanup
  namespace: vlog
subjects:
- kind: ServiceAccount
  name: pod-cleanup
  namespace: vlog
roleRef:
  kind: Role
  name: pod-cleanup
  apiGroup: rbac.authorization.k8s.io
