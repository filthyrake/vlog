#!/usr/bin/env python3
"""
Legacy CLI script wrapper - use 'vlog' command instead.
This script is kept for backward compatibility with existing scripts.
Run 'pip install -e .' from the repository root to install the vlog command.
"""
import sys
from cli.main import main

if __name__ == "__main__":
    main()
